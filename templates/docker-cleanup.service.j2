[Unit]
Description=Docker cleanup
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
WorkingDirectory=/tmp
User=root
Group=root
ExecStart={{ docker_extra_cleanup_docker_bin }} system prune -af

[Install]
WantedBy=multi-user.target